const categorySchema = require('./Category.json');
const transactionSchema = require('./Transaction.json');

export const JSONSchema = {
  Category: categorySchema,
  Transaction: transactionSchema,
};
